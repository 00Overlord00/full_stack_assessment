console.log( 'SCRIPT: GO' );

var wheelOfFortune = function() {   //Random # Generator
  return Math.random() * ( 1, 100 ) + 1;
};

var randomFrickinNumber = function() {  //Random # Generator is then housed inside another function that parseInts the value.
  return parseInt( wheelOfFortune() );
};

$(document).ready(function() {
  console.log( 'JQUERY: GO' );

$( '#creator' ).on( 'click', function(){  //On press of #creator button:
  var newCreature = $( '#inputField' ).val();  //Variable created to house data from input field.
  var population = randomFrickinNumber();  //Variable created to signify population, number is generated by Random # Generator.

  var creatureInfo = {
    "name": newCreature,  //This is an object intended to house the given data.
    "quantity": population
  };  //End object
console.log( 'Sending package: ' + creatureInfo.name + ' ' + creatureInfo.quantity );
  $.ajax({
    type: 'POST',
    url: '/postPath',  //ajax call.
    data: creatureInfo
  });  //End ajax call.
  $( '#displayArea' ).empty();

  // $.ajax({
  //   type: 'GET',
  //   url: '/getPath',
  //   success: function( data ) {
  //     var dataIn = data;
  //     appendingFunction( dataIn );
  //   }  //End Success.
  // });  //End ajax.

  $( '#inputField' ).empty();
});  //End button click.

var appendingFunction = function( critters ) {
  for( i = 0; i < critters.length; i++) {
    var displayAnimal = '<p>Species Name: ' + critters[i].creature + ', Current Population: ' + critters[i].population + '</p>';
    console.log( displayAnimal );
    $( '#appendingArea' ).append( displayAnimal );
  }
};


});
